---
phase: 05-autonomous-cycle-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - polymarket-trader/.claude/CLAUDE.md
  - polymarket-trader/tests/test_strategy_evolution.py
  - polymarket-trader/lib/market_data.py
autonomous: true
requirements:
  - STRAT-03
  - STRAT-04
  - STRAT-05
  - STRAT-06
  - STRAT-07

must_haves:
  truths:
    - "CLAUDE.md Phase E instructs Claude to call record_outcome.py for resolved positions"
    - "CLAUDE.md Phase E instructs Claude to update golden-rules.md after losses"
    - "CLAUDE.md Phase E instructs Claude to append Lessons Learned to category playbooks"
    - "CLAUDE.md Phase B uses --limit 50 and 0.10-0.85 price range"
    - "CLAUDE.md Phase B references 14-day max resolution window"
    - "Gamma API batch size in lib/market_data.py is 200 or higher"
    - "test_strategy_starts_blank passes with current strategy.md"
  artifacts:
    - path: "polymarket-trader/.claude/CLAUDE.md"
      provides: "Updated Phase B and Phase E instructions"
      contains: "record_outcome.py"
    - path: "polymarket-trader/tests/test_strategy_evolution.py"
      provides: "Fixed test assertion"
      contains: "starts blank"
    - path: "polymarket-trader/lib/market_data.py"
      provides: "Increased Gamma API batch size"
      contains: "limit"
  key_links:
    - from: "polymarket-trader/.claude/CLAUDE.md"
      to: "polymarket-trader/tools/record_outcome.py"
      via: "Phase E step 2 CLI instruction"
      pattern: "record_outcome.py --market-id"
    - from: "polymarket-trader/.claude/CLAUDE.md"
      to: "polymarket-trader/knowledge/golden-rules.md"
      via: "Phase E step 6 instruction"
      pattern: "golden-rules.md"
    - from: "polymarket-trader/.claude/CLAUDE.md"
      to: "polymarket-trader/knowledge/market-types/"
      via: "Phase E step 7 instruction"
      pattern: "market-types/"
    - from: "polymarket-trader/lib/market_data.py"
      to: "polymarket-trader/tools/discover_markets.py"
      via: "fetch_active_markets called by discover CLI"
      pattern: "fetch_active_markets"
---

<objective>
Update CLAUDE.md with explicit Phase E instructions for golden-rules updates, playbook evolution,
and record_outcome.py invocation. Tune Phase B discovery parameters to fix zero-trade cycles.
Increase Gamma API batch size in lib/market_data.py to ensure enough raw markets pass filters.
Fix broken test_strategy_starts_blank assertion.

Purpose: Without these CLAUDE.md updates, Claude sessions lack explicit instructions to update
golden-rules.md (STRAT-04), call record_outcome.py (STRAT-05), or evolve playbooks (STRAT-06).
Without discovery tuning (both CLAUDE.md and API batch size), cycles produce zero tradeable markets.
The test fix prevents CI failures.

Output: Updated CLAUDE.md, increased API batch size in market_data.py, fixed test file. Enables productive trading cycles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-autonomous-cycle-validation/05-CONTEXT.md
@.planning/phases/05-autonomous-cycle-validation/05-RESEARCH.md

@polymarket-trader/.claude/CLAUDE.md
@polymarket-trader/tests/test_strategy_evolution.py
@polymarket-trader/state/strategy.md
@polymarket-trader/tools/record_outcome.py
@polymarket-trader/lib/market_data.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md Phase B and Phase E instructions, increase Gamma API batch size</name>
  <files>polymarket-trader/.claude/CLAUDE.md, polymarket-trader/lib/market_data.py</files>
  <read_first>
    - polymarket-trader/.claude/CLAUDE.md (current state -- modification target)
    - polymarket-trader/tools/record_outcome.py (CLI interface -- must match exactly)
    - polymarket-trader/.claude/skills/post-mortem.md (golden-rule creation framework)
    - polymarket-trader/.claude/skills/cycle-review.md (cycle report template)
    - polymarket-trader/lib/config.py (max_resolution_days=14 confirmation)
    - polymarket-trader/lib/market_data.py (Gamma API batch size -- currently hardcoded to 50)
  </read_first>
  <action>
**Part A: CLAUDE.md Phase B and Phase E updates**

Edit polymarket-trader/.claude/CLAUDE.md with these exact changes:

**Phase B (Find Opportunities) -- replace lines 51-55 (steps 1-5) with:**

```
1. Run: `python tools/discover_markets.py --limit 50 --pretty`
2. Apply resolution filter: exclude markets resolving beyond 14 days (MAX_RESOLUTION_DAYS in config)
3. Apply sweet-spot filter: YES price between 0.10-0.85
4. Remove markets where we already hold positions (from Phase A portfolio)
5. Rank by volume_24h descending, take top 5-8 candidates
6. If fewer than 3 pass, relax price range to 0.05-0.95
```

Key changes per D-07: `--limit 20` becomes `--limit 50`, price sweet spot widens from `0.15-0.85` to `0.10-0.85`, explicit 14-day resolution filter added as step 2 (shifts old steps down), relaxed fallback widens to `0.05-0.95`.

**Phase E (Learn and Evolve) -- replace lines 85-90 (steps 1-6) with:**

```
1. Load `.claude/skills/post-mortem.md` for any resolved positions from Phase A
2. Load `.claude/skills/calibration-check.md` to record outcomes and update calibration data
   - For each resolved position, run: `python tools/record_outcome.py --market-id {id} --stated-prob {prob} --actual {WIN|LOSS} --category {cat} --pnl {pnl} --pretty`
   - Then interpret results using the calibration-check framework
3. Load `.claude/skills/cycle-review.md` to write the cycle report and update strategy
4. Write cycle report to `state/reports/cycle-{cycle_id}.md`
5. Update `state/strategy.md` with max 0-3 evidence-backed changes
6. If any post-mortem yielded a golden-rule candidate (loss > 2% bankroll OR 2+ repeated patterns): add to `knowledge/golden-rules.md` following its format. Max 3 new rules per cycle.
7. For each category traded this cycle: append a dated "Lessons Learned" entry to `knowledge/market-types/{category}.md` with observation and evidence
8. Log cycle summary: markets scanned, analyzed, traded, P&L impact
```

Key changes: Step 2 adds explicit record_outcome.py CLI call per D-02. Step 6 adds golden-rules.md update trigger per D-01/D-10. Step 7 adds playbook evolution per D-01/D-11. Old step 6 becomes step 8.

Do NOT modify any `<!-- OPERATOR-SET -->` markers -- the content under them IS operator-set content being updated by the operator (us). Do NOT modify the Guardrails section, Session Start, Error Handling, or Process Notes sections.

**Part B: Increase Gamma API batch size in lib/market_data.py**

The Gamma API batch size is hardcoded to 50 at line 45 of `lib/market_data.py`. The `--limit` flag on `discover_markets.py` only controls post-filter truncation, NOT the API fetch. With only 50 raw markets fetched, after applying volume, liquidity, price range, and resolution filters, very few (often zero) markets survive.

Edit `polymarket-trader/lib/market_data.py`: In the `fetch_active_markets()` function, change the `"limit": 50` parameter in the `requests.get()` params dict to `"limit": 200`. Update the comment to: `# fetch broad set, filter locally`.

This ensures 200 raw markets are fetched from Gamma, giving the post-filters enough candidates to produce tradeable markets.
  </action>
  <verify>
    <automated>cd /home/trader/polymarket-trader && grep -c "record_outcome.py" .claude/CLAUDE.md && grep -c "golden-rules.md" .claude/CLAUDE.md && grep -c "market-types/" .claude/CLAUDE.md && grep "limit 50" .claude/CLAUDE.md && grep "0.10-0.85" .claude/CLAUDE.md && grep "14 days" .claude/CLAUDE.md && echo "--- Phase E step count ---" && grep -A 20 "Phase E" .claude/CLAUDE.md | grep -cE "^[0-9]+\." && echo "--- Section integrity ---" && grep "Guardrails" .claude/CLAUDE.md && grep "Session Start" .claude/CLAUDE.md && echo "--- API batch size ---" && grep '"limit": 200' lib/market_data.py</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md Phase B step 1 contains `--limit 50`
    - CLAUDE.md Phase B contains `0.10-0.85` price range
    - CLAUDE.md Phase B contains `14 days` resolution filter
    - CLAUDE.md Phase E contains `python tools/record_outcome.py --market-id {id} --stated-prob {prob} --actual {WIN|LOSS} --category {cat} --pnl {pnl} --pretty`
    - CLAUDE.md Phase E contains `knowledge/golden-rules.md` reference with loss threshold
    - CLAUDE.md Phase E contains `knowledge/market-types/{category}.md` with "Lessons Learned"
    - CLAUDE.md Phase E has 8 numbered steps (was 6)
    - Guardrails section unchanged
    - Session Start section unchanged
    - lib/market_data.py has `"limit": 200` (was 50) in fetch_active_markets params
  </acceptance_criteria>
  <done>CLAUDE.md Phase B uses --limit 50, 0.10-0.85 range, 14-day filter. Phase E has record_outcome.py call, golden-rules update trigger, playbook evolution instruction. All 8 steps present. Gamma API batch size increased to 200 in lib/market_data.py.</done>
</task>

<task type="auto">
  <name>Task 2: Fix test_strategy_starts_blank assertion</name>
  <files>polymarket-trader/tests/test_strategy_evolution.py</files>
  <read_first>
    - polymarket-trader/tests/test_strategy_evolution.py (current test -- modification target)
    - polymarket-trader/state/strategy.md (current content -- assertion must match)
    - polymarket-trader/state/core-principles.md (verify core_principles_has_placeholder still valid)
  </read_first>
  <action>
Edit polymarket-trader/tests/test_strategy_evolution.py:

1. Update `test_strategy_starts_blank` (line 17-25): Replace the assertion that checks for "No rules yet" or "No approach defined" with an assertion that checks strategy.md contains only section headers and no actual rules. The current strategy.md has this preamble text: "This document is updated by the trading agent after each cycle." and section headers like "## Market Selection Rules" with empty content below them.

New assertion logic:
```python
def test_strategy_starts_blank():
    """strategy.md contains no pre-seeded rules -- only headers and preamble (STRT-01)."""
    strategy_path = os.path.join(PROJECT_ROOT, "state", "strategy.md")
    assert os.path.isfile(strategy_path), "state/strategy.md does not exist"
    content = open(strategy_path).read()
    # Should have section headers but no bullet-point rules underneath them
    lines = [l.strip() for l in content.strip().splitlines() if l.strip()]
    rule_lines = [l for l in lines if l.startswith("- ") or l.startswith("* ")]
    assert len(rule_lines) == 0, (
        f"strategy.md should have no pre-seeded rules, found {len(rule_lines)}: {rule_lines[:3]}"
    )
```

2. Also check `test_core_principles_has_placeholder` (line 57-62): Read core-principles.md to verify it still contains "to be defined". If it does not (Phase 2 rewrote it to immutable guardrails), update the test to check for "never modified" instead which is already in `test_core_principles_separate`. If this test is now redundant with `test_core_principles_separate`, remove `test_core_principles_has_placeholder` entirely.
  </action>
  <verify>
    <automated>cd /home/trader/polymarket-trader && source .venv/bin/activate && python -m pytest tests/test_strategy_evolution.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_strategy_evolution.py -x` exits 0
    - test_strategy_starts_blank passes (no longer checks for "No rules yet")
    - test_strategy_has_four_domains passes
    - test_core_principles_separate passes
    - Full test suite still passes: `python -m pytest tests/ -x` exits 0
  </acceptance_criteria>
  <done>All tests in test_strategy_evolution.py pass. test_strategy_starts_blank validates no pre-seeded rules exist (checks for absence of bullet lines). Full test suite green.</done>
</task>

</tasks>

<verification>
1. `grep "record_outcome.py" polymarket-trader/.claude/CLAUDE.md` shows the CLI call in Phase E
2. `grep "limit 50" polymarket-trader/.claude/CLAUDE.md` confirms discovery tuning
3. `grep "golden-rules.md" polymarket-trader/.claude/CLAUDE.md` shows golden-rule update instruction in Phase E
4. `grep "market-types" polymarket-trader/.claude/CLAUDE.md` shows playbook evolution instruction
5. `grep '"limit": 200' polymarket-trader/lib/market_data.py` confirms API batch size increase
6. `cd polymarket-trader && source .venv/bin/activate && python -m pytest tests/ -x` -- full suite green
</verification>

<success_criteria>
- CLAUDE.md Phase B discovery uses --limit 50, 0.10-0.85 price range, 14-day resolution filter
- CLAUDE.md Phase E has explicit record_outcome.py CLI call, golden-rules.md update trigger, playbook evolution instruction
- CLAUDE.md Phase E has exactly 8 numbered steps
- lib/market_data.py Gamma API batch size is 200 (was 50)
- test_strategy_evolution.py passes completely
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/05-autonomous-cycle-validation/05-01-SUMMARY.md`
</output>
