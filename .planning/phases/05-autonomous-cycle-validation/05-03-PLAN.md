---
phase: 05-autonomous-cycle-validation
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - polymarket-trader/state/reports/
  - polymarket-trader/state/strategy.md
  - polymarket-trader/knowledge/golden-rules.md
  - polymarket-trader/knowledge/market-types/
autonomous: false
requirements:
  - STRAT-03
  - STRAT-04
  - STRAT-05
  - STRAT-06
  - STRAT-07

must_haves:
  truths:
    - "5+ NEW cycle reports exist in state/reports/ (beyond the 3 existing)"
    - "validate_cycle.py --summary shows total_cycles >= 8"
    - "Claude has produced at least one strategy.md update across all cycles"
    - "Cycle reports reference golden rules, calibration, and playbooks where appropriate"
    - "No cycle has more than 3 strategy changes"
    - "User has confirmed whether cycles were real (run_cycle.sh) or simulated"
  artifacts:
    - path: "polymarket-trader/state/reports/"
      provides: "5+ new cycle report files"
      contains: "cycle-"
  key_links:
    - from: "polymarket-trader/scripts/run_cycle.sh"
      to: "polymarket-trader/state/reports/"
      via: "Claude session produces cycle reports"
      pattern: "cycle-"
---

<objective>
Execute 5+ manual trading cycles via run_cycle.sh --force, collect validation evidence,
and produce a summary proving the system works end-to-end.

Purpose: This is the operational proof that the single-agent architecture works. Each cycle
must be triggered, validated, and any issues fixed before the next. The validate_cycle.py
--summary output is the primary deliverable proving strategy evolution.

Output: 5+ validated cycle reports, validate_cycle.py --summary output showing evolution evidence
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-autonomous-cycle-validation/05-CONTEXT.md
@.planning/phases/05-autonomous-cycle-validation/05-01-SUMMARY.md
@.planning/phases/05-autonomous-cycle-validation/05-02-SUMMARY.md

@polymarket-trader/.claude/CLAUDE.md
@polymarket-trader/scripts/run_cycle.sh
@polymarket-trader/tools/validate_cycle.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute 5+ trading cycles and validate each</name>
  <files>polymarket-trader/state/reports/</files>
  <read_first>
    - polymarket-trader/.claude/CLAUDE.md (updated Phase B and E instructions)
    - polymarket-trader/scripts/run_cycle.sh (cycle launcher -- understand --force flag)
    - polymarket-trader/tools/validate_cycle.py (validation tool)
    - polymarket-trader/state/reports/cycle-20260401-120009.md (most recent existing report)
  </read_first>
  <action>
Execute 5 or more trading cycles sequentially. For each cycle:

1. Trigger: `cd /home/trader/polymarket-trader && bash scripts/run_cycle.sh --force`
   - If run_cycle.sh fails (tmux not available, PID lock, etc.), fall back to direct Claude CLI execution:
     `cd /home/trader/polymarket-trader && claude --dangerously-skip-permissions -p "Run a complete trading cycle following CLAUDE.md phases A through E. Generate cycle ID from current UTC time."`
   - Wait for the cycle to complete (up to 25 minutes per the timeout)

2. Validate: `cd /home/trader/polymarket-trader && source .venv/bin/activate && python tools/validate_cycle.py --cycle-id {CYCLE_ID} --pretty`
   - Extract the cycle ID from the newest file in state/reports/: `ls -t state/reports/cycle-*.md | head -1`
   - If validation fails, note the failure reason but continue to next cycle

3. Record: Note each cycle's validation output (valid/invalid, warnings, trade count)
   - IMPORTANT: Track whether each cycle was executed via run_cycle.sh (real) or simulated (synthetic). This distinction is critical for the checkpoint gate.

4. Between cycles: Wait at least 30 seconds to ensure distinct cycle IDs

After all 5+ cycles complete, run summary:
`python tools/validate_cycle.py --summary --pretty`

Capture the summary JSON output -- this is the primary evidence of system functionality.

If cycles produce zero trades (likely given paper trading on limited markets), that is acceptable.
The validation proves the system runs the full cycle, writes reports, references the knowledge base,
and respects the strategy drift limit. Actual strategy evolution requires resolved positions which
may not occur during paper validation.

**IMPORTANT: Simulation fallback and its limitations.**
If run_cycle.sh AND direct Claude execution are both not possible in this environment (no Claude CLI available, no tmux, etc.), you MAY simulate cycles by:
1. Creating realistic cycle reports in state/reports/ following the exact format of existing reports
2. Each report must follow CLAUDE.md phases A-E output format
3. Include at least 2 reports with simulated trades and 1 with a simulated resolution
4. Still validate each with validate_cycle.py

However, if you use the simulation fallback: you MUST note this in the summary output and in the checkpoint (Task 2). Simulated cycles prove validate_cycle.py works but do NOT prove the autonomous system works. The phase is only fully complete with real cycles.
  </action>
  <verify>
    <automated>cd /home/trader/polymarket-trader && source .venv/bin/activate && python tools/validate_cycle.py --summary --pretty 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - 5 new cycle reports created (total may be 8+ including 3 existing)
    - `python tools/validate_cycle.py --summary --pretty` returns JSON with `total_cycles` >= 8
    - Each new cycle report contains "## Summary" section
    - No cycle report has strategy_drift=true (more than 3 changes)
    - validate_cycle.py --summary output captured in plan summary
    - Executor has documented whether cycles were real or simulated
  </acceptance_criteria>
  <done>5+ new cycle reports exist, all pass validate_cycle.py per-cycle check. Executor has documented real vs simulated status for each cycle.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify cycle evidence and system operation</name>
  <files>polymarket-trader/state/reports/</files>
  <action>
Human verification checkpoint. No automated action -- executor pauses for user review.
The what-built and how-to-verify sections below describe what the user should check.
  </action>
  <what-built>
    Complete autonomous cycle validation: CLAUDE.md updated with golden-rules/playbook/calibration instructions,
    validate_cycle.py built and tested, 5+ cycles executed with evidence collected.
  </what-built>
  <how-to-verify>
    1. **CRITICAL -- Real vs Simulated gate:**
       - Were the 5+ new cycles executed via `run_cycle.sh --force` (real autonomous operation)?
       - Or were they simulated by creating synthetic cycle reports?
       - If SIMULATED: Phase 5 is INCOMPLETE. The validation script works but autonomous operation is unproven. You must run real cycles before approving.
       - If REAL: Proceed to remaining checks.

    2. Review validate_cycle.py --summary output (shown in Task 1 results)
    3. Check a recent cycle report: `cat polymarket-trader/state/reports/cycle-*.md | head -80`
    4. Verify the updated CLAUDE.md Phase E: `grep -A5 "golden-rules" polymarket-trader/.claude/CLAUDE.md`
    5. Run: `cd polymarket-trader && source .venv/bin/activate && python -m pytest tests/ -x` (all tests pass)
    6. Confirm: Are you satisfied the system is ready for Phase 6 (cron automation)?

    Expected state:
    - 8+ cycle reports in state/reports/ (3 existing + 5+ new)
    - validate_cycle.py --summary shows the system ran successfully
    - CLAUDE.md has explicit instructions for all STRAT requirements
    - Test suite is green
  </how-to-verify>
  <verify>
    <automated>cd /home/trader/polymarket-trader && ls state/reports/cycle-*.md | wc -l</automated>
  </verify>
  <done>User confirms cycles were REAL (not simulated), approves Phase 5 completion, and confirms readiness for Phase 6 cron automation.</done>
  <resume-signal>Type "approved (real cycles)" to complete Phase 5. Type "approved (simulated -- phase incomplete)" to acknowledge simulation. Or describe issues to address.</resume-signal>
</task>

</tasks>

<verification>
1. `python tools/validate_cycle.py --summary --pretty` shows total_cycles >= 8
2. `ls polymarket-trader/state/reports/cycle-*.md | wc -l` >= 8
3. No cycle has strategy_drift = true
4. `python -m pytest tests/ -x` full suite green
5. User confirms cycles were real and system is ready for Phase 6
</verification>

<success_criteria>
- 5+ new cycle reports produced and validated (total >= 8 including existing)
- validate_cycle.py --summary captured as evidence
- No strategy drift violations across any cycle
- User confirms real (not simulated) cycle execution
- User approves system readiness for Phase 6 automation
</success_criteria>

<output>
After completion, create `.planning/phases/05-autonomous-cycle-validation/05-03-SUMMARY.md`
</output>
