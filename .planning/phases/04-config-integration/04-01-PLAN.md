---
phase: 04-config-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - polymarket-trader/lib/config.py
  - polymarket-trader/.env
  - polymarket-trader/.env.example
  - polymarket-trader/requirements.txt
  - polymarket-trader/setup_wallet.py
  - polymarket-trader/state/bankroll.json
  - polymarket-trader/CLAUDE.md
  - polymarket-trader/.claude/CLAUDE.md
  - polymarket-trader/tests/test_config.py
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03, CONF-04]

must_haves:
  truths:
    - "No import openai or import anthropic anywhere in codebase Python files"
    - ".env.example has ALPHA_VANTAGE_API_KEY and no OPENAI_API_KEY"
    - "Config dataclass has max_position_pct, max_exposure_pct, max_resolution_days instead of dollar fields"
    - "load_bankroll() reads state/bankroll.json and returns balance_usdc"
    - "MIN_EDGE_THRESHOLD default is 0.04"
    - "state/bankroll.json exists with default 10000 balance"
  artifacts:
    - path: "polymarket-trader/lib/config.py"
      provides: "Percentage-based Config with bankroll loading"
      contains: "max_position_pct"
    - path: "polymarket-trader/state/bankroll.json"
      provides: "Runtime bankroll source"
      contains: "balance_usdc"
    - path: "polymarket-trader/tests/test_config.py"
      provides: "Config and bankroll loading tests"
  key_links:
    - from: "polymarket-trader/lib/config.py"
      to: "polymarket-trader/state/bankroll.json"
      via: "load_bankroll() reads JSON file"
      pattern: "load_bankroll"
---

<objective>
Remove OpenAI dependency, migrate config from dollar-based to percentage-based sizing with bankroll.json, widen trading parameters, and clean all stale OpenAI references.

Purpose: Claude IS the AI runtime -- no Python SDK needed. Percentage-based sizing enables bankroll-relative position management matching core-principles.md.
Output: Clean config.py with percentage fields + bankroll loading, updated .env files, removed openai/anthropic packages, bankroll.json seed file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-config-integration/04-CONTEXT.md
@polymarket-trader/lib/config.py
@polymarket-trader/.env.example
@polymarket-trader/state/core-principles.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Overhaul config.py — percentage-based sizing, bankroll loading, widened parameters</name>
  <files>polymarket-trader/lib/config.py, polymarket-trader/state/bankroll.json, polymarket-trader/tests/test_config.py</files>
  <read_first>
    polymarket-trader/lib/config.py
    polymarket-trader/state/core-principles.md
    polymarket-trader/.claude/skills/size-position.md
    polymarket-trader/tests/test_config.py
  </read_first>
  <behavior>
    - Test: Config() has field max_position_pct with default 0.05
    - Test: Config() has field max_exposure_pct with default 0.30
    - Test: Config() has field max_resolution_days with default 14
    - Test: Config() has field min_edge_threshold with default 0.04
    - Test: Config() does NOT have field max_position_size_usdc
    - Test: Config() does NOT have field max_total_exposure_usdc
    - Test: load_bankroll() returns {"balance_usdc": 10000, "updated": "..."} from state/bankroll.json
    - Test: load_bankroll() returns {"balance_usdc": 10000, "updated": "..."} when file missing (safe default)
    - Test: load_bankroll() returns safe default when JSON is corrupted
    - Test: _ENV_MAP has MAX_POSITION_PCT, MAX_EXPOSURE_PCT, MAX_RESOLUTION_DAYS
    - Test: _ENV_MAP does NOT have MAX_POSITION_SIZE_USDC or MAX_TOTAL_EXPOSURE_USDC
  </behavior>
  <action>
    Modify polymarket-trader/lib/config.py per D-03, D-10, D-11, D-12:

    1. In Config dataclass, REMOVE these two fields:
       - max_position_size_usdc: float = 50.0
       - max_total_exposure_usdc: float = 200.0

    2. In Config dataclass, ADD these three fields:
       - max_position_pct: float = 0.05
       - max_exposure_pct: float = 0.30
       - max_resolution_days: int = 14

    3. CHANGE default for min_edge_threshold from 0.10 to 0.04

    4. In _ENV_MAP, REMOVE:
       - "MAX_POSITION_SIZE_USDC": "max_position_size_usdc"
       - "MAX_TOTAL_EXPOSURE_USDC": "max_total_exposure_usdc"

    5. In _ENV_MAP, ADD:
       - "MAX_POSITION_PCT": "max_position_pct"
       - "MAX_EXPOSURE_PCT": "max_exposure_pct"
       - "MAX_RESOLUTION_DAYS": "max_resolution_days"

    6. Add load_bankroll() function (per D-03, D-04). Place it in config.py below load_config():

       ```python
       import json
       from pathlib import Path

       _BANKROLL_PATH = Path(__file__).resolve().parent.parent / "state" / "bankroll.json"
       _DEFAULT_BANKROLL = 10000.0

       def load_bankroll(path: Path | None = None) -> dict:
           """Load bankroll from state/bankroll.json. Returns safe default if missing/corrupt."""
           bp = path or _BANKROLL_PATH
           try:
               with open(bp) as f:
                   data = json.load(f)
               balance = float(data["balance_usdc"])
               return {"balance_usdc": balance, "updated": data.get("updated", "")}
           except (FileNotFoundError, json.JSONDecodeError, KeyError, ValueError):
               from datetime import datetime, timezone
               return {"balance_usdc": _DEFAULT_BANKROLL, "updated": datetime.now(timezone.utc).isoformat()}
       ```

    7. Create polymarket-trader/state/bankroll.json with content:
       ```json
       {"balance_usdc": 10000, "updated": "2026-04-04T00:00:00Z"}
       ```

    8. Write tests in polymarket-trader/tests/test_config.py covering the behaviors listed above. Use tmp_path fixture for bankroll file tests to avoid modifying the real file.
  </action>
  <verify>
    <automated>cd /home/trader/polymarket-trader && source .venv/bin/activate && python -m pytest tests/test_config.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - polymarket-trader/lib/config.py contains `max_position_pct: float = 0.05`
    - polymarket-trader/lib/config.py contains `max_exposure_pct: float = 0.30`
    - polymarket-trader/lib/config.py contains `max_resolution_days: int = 14`
    - polymarket-trader/lib/config.py contains `min_edge_threshold: float = 0.04`
    - polymarket-trader/lib/config.py does NOT contain `max_position_size_usdc`
    - polymarket-trader/lib/config.py does NOT contain `max_total_exposure_usdc`
    - polymarket-trader/lib/config.py contains `def load_bankroll(`
    - polymarket-trader/lib/config.py contains `_DEFAULT_BANKROLL = 10000.0`
    - polymarket-trader/state/bankroll.json contains `"balance_usdc": 10000`
    - polymarket-trader/tests/test_config.py exits 0
  </acceptance_criteria>
  <done>Config dataclass uses percentage fields, dollar fields removed, load_bankroll() works with real file and safe defaults, all config tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Remove OpenAI/Anthropic dependencies and clean all references</name>
  <files>polymarket-trader/requirements.txt, polymarket-trader/.env, polymarket-trader/.env.example, polymarket-trader/setup_wallet.py, polymarket-trader/CLAUDE.md, polymarket-trader/.claude/CLAUDE.md</files>
  <read_first>
    polymarket-trader/requirements.txt
    polymarket-trader/.env
    polymarket-trader/.env.example
    polymarket-trader/setup_wallet.py
    polymarket-trader/CLAUDE.md
    polymarket-trader/.claude/CLAUDE.md
  </read_first>
  <action>
    Per D-01, D-02, D-13:

    1. polymarket-trader/requirements.txt — Remove these two lines:
       - `anthropic>=0.49.0`
       - The `openai` line if present (check — context says no openai import but it might be in requirements)
       Keep all other packages. Final contents should be:
       ```
       py-clob-client>=0.17.0
       requests>=2.31.0
       python-dotenv>=1.0.0
       eth-account>=0.13.0
       web3>=7.0.0
       ```

    2. polymarket-trader/.env — Remove these lines:
       - `# OpenAI API key`
       - `OPENAI_API_KEY=sk-proj-REDACTED`

       Update risk parameters to match new config:
       - Remove `MAX_POSITION_SIZE_USDC=50`
       - Remove `MAX_TOTAL_EXPOSURE_USDC=200`
       - Add `MAX_POSITION_PCT=0.05`
       - Add `MAX_EXPOSURE_PCT=0.30`
       - Add `MAX_RESOLUTION_DAYS=14`
       - Change `MIN_EDGE_THRESHOLD=0.10` to `MIN_EDGE_THRESHOLD=0.04`
       - Add `ALPHA_VANTAGE_API_KEY=` (empty, user fills in)

    3. polymarket-trader/.env.example — Full rewrite matching new config:
       ```
       # Polymarket Autonomous Trading Agent Configuration
       # Copy to .env and fill in required values

       # === REQUIRED ===
       # PRIVATE_KEY=your_ethereum_private_key  # Only needed for live trading
       # ALPHA_VANTAGE_API_KEY=your_key_here    # For market intelligence

       # === TRADING MODE ===
       PAPER_TRADING=true  # Set to "false" for live trading (requires PRIVATE_KEY)

       # === RISK PARAMETERS ===
       MIN_EDGE_THRESHOLD=0.04      # Minimum edge to consider a trade (4pp)
       KELLY_FRACTION=0.25          # Quarter Kelly for conservative sizing
       MAX_POSITION_PCT=0.05        # Maximum single position as fraction of bankroll
       MAX_EXPOSURE_PCT=0.30        # Maximum total exposure as fraction of bankroll
       MAX_RESOLUTION_DAYS=14       # Maximum days to resolution for market consideration

       # === MARKET DISCOVERY FILTERS ===
       MIN_VOLUME_24H=1000          # Minimum 24h volume in USDC
       MIN_LIQUIDITY=500            # Minimum liquidity in USDC
       MAX_MARKETS_PER_CYCLE=10     # Maximum markets to return per discovery

       # === API ENDPOINTS ===
       POLYMARKET_HOST=https://clob.polymarket.com
       GAMMA_API_URL=https://gamma-api.polymarket.com
       CHAIN_ID=137                 # Polygon mainnet

       # === DATABASE ===
       DB_PATH=trading.db

       # === LOGGING ===
       LOG_LEVEL=INFO               # DEBUG, INFO, WARNING, ERROR
       LOG_FILE=trading.log         # Structured JSON log file

       # === SCHEDULING ===
       CYCLE_INTERVAL=4h               # Trading cycle interval
       MIN_PAPER_CYCLES=10             # Minimum paper cycles before live trading gate opens
       ```

    4. polymarket-trader/setup_wallet.py line 154 — Change:
       `print("2. Set OPENAI_API_KEY in .env")`
       to:
       `print("2. Set ALPHA_VANTAGE_API_KEY in .env (for market intelligence)")`

    5. polymarket-trader/CLAUDE.md — In the Technology Stack / Key Dependencies section, remove ALL references to openai, OpenAI, GPT-4o, OPENAI_API_KEY. Replace the market_analyzer.py layer description: change "OpenAI GPT-4o" to "Claude Code CLI" and remove openai dependency. Remove `OPENAI_API_KEY required for market analysis` from Platform Requirements. Remove anthropic from Frameworks list. Change the `OPENAI_API_KEY` constant reference in Naming Patterns to `ALPHA_VANTAGE_API_KEY`. In the Analysis layer description, change "OpenAI" references to "Claude (via Claude Code CLI session)".

    6. polymarket-trader/.claude/CLAUDE.md — The agent-facing CLAUDE.md. Scan for any "OpenAI" mentions and remove them. Based on current content, there appear to be none, but verify and clean if found.

    7. Run the full test suite to confirm no regressions from dependency removal.
  </action>
  <verify>
    <automated>cd /home/trader/polymarket-trader && source .venv/bin/activate && python -m pytest tests/ -x --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - polymarket-trader/requirements.txt does NOT contain `openai` or `anthropic`
    - polymarket-trader/requirements.txt contains `py-clob-client>=0.17.0`
    - polymarket-trader/.env does NOT contain `OPENAI_API_KEY`
    - polymarket-trader/.env contains `ALPHA_VANTAGE_API_KEY=`
    - polymarket-trader/.env contains `MAX_POSITION_PCT=0.05`
    - polymarket-trader/.env does NOT contain `MAX_POSITION_SIZE_USDC`
    - polymarket-trader/.env.example contains `ALPHA_VANTAGE_API_KEY`
    - polymarket-trader/.env.example does NOT contain `OPENAI_API_KEY`
    - polymarket-trader/.env.example contains `MAX_POSITION_PCT=0.05`
    - polymarket-trader/.env.example contains `MAX_RESOLUTION_DAYS=14`
    - polymarket-trader/.env.example contains `MIN_EDGE_THRESHOLD=0.04`
    - polymarket-trader/setup_wallet.py contains `ALPHA_VANTAGE_API_KEY`
    - polymarket-trader/setup_wallet.py does NOT contain `OPENAI_API_KEY`
    - polymarket-trader/CLAUDE.md does NOT contain `OPENAI_API_KEY`
    - All existing tests pass (exit 0)
  </acceptance_criteria>
  <done>Zero OpenAI/Anthropic references in code and config, .env.example reflects new percentage-based config with ALPHA_VANTAGE_API_KEY, all tests pass</done>
</task>

</tasks>

<verification>
- `grep -r "openai\|OPENAI" polymarket-trader/ --include="*.py" --include="*.txt" --include=".env*"` returns no matches
- `grep -r "anthropic" polymarket-trader/requirements.txt` returns no matches
- `python -c "from lib.config import load_config, load_bankroll; c = load_config(); print(c.max_position_pct, c.max_exposure_pct, c.max_resolution_days)"` prints `0.05 0.30 14`
- `python -c "from lib.config import load_bankroll; print(load_bankroll())"` prints dict with balance_usdc 10000
- Full test suite passes
</verification>

<success_criteria>
- Config uses percentage-based sizing (max_position_pct=0.05, max_exposure_pct=0.30) with no dollar fields
- load_bankroll() reads state/bankroll.json with safe $10k default
- MAX_RESOLUTION_DAYS=14 and MIN_EDGE_THRESHOLD=0.04 as defaults
- Zero openai/anthropic/OpenAI references in Python code, requirements, env files
- ALPHA_VANTAGE_API_KEY in .env.example
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-config-integration/04-01-SUMMARY.md`
</output>
