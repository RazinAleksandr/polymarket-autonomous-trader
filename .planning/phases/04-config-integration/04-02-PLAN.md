---
phase: 04-config-integration
plan: 02
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - polymarket-trader/scripts/run_cycle.sh
  - polymarket-trader/tests/test_run_cycle.py
autonomous: true
requirements: [SCHED-04]

must_haves:
  truths:
    - "run_cycle.sh reads state/signal.json and exits 0 when all flags are false"
    - "run_cycle.sh launches Claude Code CLI session when any flag is true"
    - "run_cycle.sh uses PID file locking to prevent concurrent cycles"
    - "run_cycle.sh manages tmux sessions for background execution"
    - "run_cycle.sh enforces 20-minute timeout on Claude sessions"
  artifacts:
    - path: "polymarket-trader/scripts/run_cycle.sh"
      provides: "Heartbeat-gated cycle launcher"
      contains: "signal.json"
---

<objective>
Create run_cycle.sh -- a heartbeat-gated cycle launcher that reads signal.json and only launches expensive Claude trading sessions when the heartbeat indicates work is needed.

Purpose: Cron calls run_cycle.sh every 30 minutes. If heartbeat says nothing to do (all flags false), exit immediately. If any flag is true, launch Claude Code CLI in a tmux session with PID locking and 20-minute timeout.
Output: run_cycle.sh with signal gating, PID locking, tmux management, and timeout.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create run_cycle.sh with heartbeat gating, PID lock, tmux, and timeout</name>
  <files>polymarket-trader/scripts/run_cycle.sh</files>
  <action>
    Create polymarket-trader/scripts/run_cycle.sh implementing:
    1. Read state/signal.json -- parse scan_needed, resolve_needed, learn_needed flags
    2. If all flags false: log "No work needed", exit 0
    3. If any flag true: proceed to launch cycle
    4. PID file locking at state/.run_cycle.pid -- check if stale, prevent concurrent runs
    5. tmux session management: create or reuse "trader" session
    6. 20-minute timeout using timeout command
    7. Launch Claude Code CLI with appropriate flags
    8. Clean up PID file on exit (trap)
    9. Log all actions to state/run_cycle.log
  </action>
  <verify>
    <automated>bash -n polymarket-trader/scripts/run_cycle.sh && echo "Syntax OK"</automated>
  </verify>
  <done>run_cycle.sh exists, is executable, passes bash syntax check</done>
</task>

<task type="auto">
  <name>Task 2: Create tests for run_cycle.sh signal gating and PID locking</name>
  <files>polymarket-trader/tests/test_run_cycle.py</files>
  <action>
    Create Python tests that verify:
    1. Script exits 0 when signal.json has all-false flags
    2. Script detects when flags are true and would launch
    3. PID locking prevents concurrent runs
    4. Stale PID files are cleaned up
    5. Missing signal.json triggers heartbeat first
  </action>
  <verify>
    <automated>cd /home/trader/polymarket-trader && source .venv/bin/activate && python -m pytest tests/test_run_cycle.py -x -v</automated>
  </verify>
  <done>All run_cycle tests pass</done>
</task>

</tasks>

<verification>
- `bash -n polymarket-trader/scripts/run_cycle.sh` passes syntax check
- Script is executable (chmod +x)
- Tests pass for signal gating logic
</verification>

<success_criteria>
- run_cycle.sh reads signal.json and exits when all flags false
- run_cycle.sh launches Claude when any flag true
- PID locking prevents concurrent runs
- tmux session management works
- 20-minute timeout enforced
- All tests pass
</success_criteria>
