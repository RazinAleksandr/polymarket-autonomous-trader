---
phase: 06-scheduling-paper-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - polymarket-trader/lib/calibration.py
  - polymarket-trader/tools/enable_live.py
  - polymarket-trader/scripts/status.sh
  - polymarket-trader/tests/test_enable_live.py
autonomous: true
requirements: [SAFE-02, VAL-05]

must_haves:
  truths:
    - "get_calibration_health() returns per-category bias data from calibration DB"
    - "enable_live.py --check runs 4 criteria (cycles >= 10, P&L > 0, win rate > 50%, no category bias > -20pp) and outputs JSON"
    - "enable_live.py --check is read-only (never writes gate pass file)"
    - "Live gate blocks if any single criterion fails"
    - "status.sh shows last heartbeat, last cycle, total cycles, recent errors"
  artifacts:
    - path: "polymarket-trader/lib/calibration.py"
      provides: "get_calibration_health() function"
      contains: "get_calibration_health"
    - path: "polymarket-trader/tools/enable_live.py"
      provides: "Enhanced live gate with --check, win rate, calibration health"
      contains: "--check"
    - path: "polymarket-trader/scripts/status.sh"
      provides: "Quick health check script"
      contains: "heartbeat"
---

<objective>
Enhance the live trading gate (enable_live.py) with win rate and calibration health checks, add a --check flag for read-only verification, add get_calibration_health() to calibration.py, and create status.sh for quick system health checks.

Purpose: The live gate must verify 4 criteria before allowing real money trading: sufficient cycles, positive P&L, >50% win rate, and healthy calibration (no category bias worse than -20pp). The status script provides a quick one-command system health check.
Output: Enhanced enable_live.py with --check flag, get_calibration_health() in calibration.py, status.sh script, and tests.
</objective>

<context>
@polymarket-trader/tools/enable_live.py (current 2-criteria gate)
@polymarket-trader/lib/calibration.py (needs get_calibration_health)
@polymarket-trader/lib/db.py (DataStore with get_paper_cycle_stats)
@.planning/phases/06-scheduling-paper-validation/06-CONTEXT.md (decisions D-05 through D-07, D-10)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add get_calibration_health() and enhance enable_live.py with --check and 4 criteria</name>
  <files>polymarket-trader/lib/calibration.py, polymarket-trader/tools/enable_live.py</files>
  <action>
    1. Add get_calibration_health(store) to lib/calibration.py:
       - Query calibration_records DB for per-category average error_pp
       - Return dict with per-category bias, overall health status, worst category
       - Category is "unhealthy" if avg error_pp < -20 (overconfident)
    
    2. Enhance tools/enable_live.py:
       - Add --check flag: runs all 4 criteria, outputs JSON to stdout, human summary to stderr
       - --check is read-only (never writes gate pass, never prompts for confirmation)
       - Criterion 1: cycles >= min_paper_cycles (existing)
       - Criterion 2: P&L > 0 (existing)
       - Criterion 3: win rate > 50% (NEW - from closed positions)
       - Criterion 4: calibration healthy (NEW - no category bias > -20pp)
       - Each criterion shows pass/fail with details
       - Overall: PASS only if all 4 pass, FAIL if any fails
       - JSON output includes all criteria results
  </action>
  <verify>
    <automated>cd /home/trader/.claude/worktrees/agent-a9a85620/polymarket-trader && source .venv/bin/activate && python -c "from lib.calibration import get_calibration_health; print('Import OK')"</automated>
  </verify>
  <done>get_calibration_health() exists and is importable, enable_live.py has --check flag with 4 criteria</done>
</task>

<task type="auto">
  <name>Task 2: Create status.sh health check script</name>
  <files>polymarket-trader/scripts/status.sh</files>
  <action>
    Create scripts/status.sh that shows:
    1. Last heartbeat time (from state/signal.json mtime)
    2. Last cycle time (from most recent state/reports/cycle-*.md)
    3. Total cycle count (count of cycle report files)
    4. Errors in last 24h (from state/run_cycle.log)
    5. Current PID lock status (from state/.run_cycle.pid)
    Quick sanity check format -- not a dashboard.
  </action>
  <verify>
    <automated>bash -n /home/trader/.claude/worktrees/agent-a9a85620/polymarket-trader/scripts/status.sh && echo "Syntax OK"</automated>
  </verify>
  <done>status.sh exists, is executable, passes bash syntax check</done>
</task>

<task type="auto">
  <name>Task 3: Create tests for enhanced enable_live.py and get_calibration_health()</name>
  <files>polymarket-trader/tests/test_enable_live.py</files>
  <action>
    Create tests verifying:
    1. get_calibration_health() returns correct structure with empty DB
    2. get_calibration_health() identifies unhealthy categories (bias < -20pp)
    3. get_calibration_health() reports healthy when all categories within bounds
    4. enable_live.py --check outputs JSON with 4 criteria
    5. enable_live.py --check fails when win rate below 50%
    6. enable_live.py --check fails when calibration unhealthy
    7. enable_live.py --check passes when all criteria met
    8. enable_live.py --check is read-only (no gate pass written)
  </action>
  <verify>
    <automated>cd /home/trader/.claude/worktrees/agent-a9a85620/polymarket-trader && source .venv/bin/activate && python -m pytest tests/test_enable_live.py -x -v</automated>
  </verify>
  <done>All tests pass</done>
</task>

</tasks>

<verification>
- `python -c "from lib.calibration import get_calibration_health"` succeeds
- `python tools/enable_live.py --check` runs without error
- `bash -n scripts/status.sh` passes syntax check
- All tests in test_enable_live.py pass
</verification>

<success_criteria>
- get_calibration_health() returns per-category bias data
- enable_live.py --check verifies 4 criteria and outputs JSON
- --check is read-only
- status.sh provides quick health overview
- All tests pass
</success_criteria>
