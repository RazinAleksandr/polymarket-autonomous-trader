---
phase: 06-scheduling-paper-validation
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified: []
autonomous: false
requirements: [VAL-01, VAL-02, VAL-03, VAL-04]

must_haves:
  truths:
    - "20+ autonomous paper trading cycles have run without human intervention"
    - "strategy.md has grown with evidence-backed changes"
    - "calibration.json is populated with accuracy tracking"
    - "golden-rules.md has expanded from trading outcomes"
    - "System ran unattended for 2+ days"
  artifacts:
    - path: "polymarket-trader/state/reports/"
      provides: "20+ cycle report files"
      contains: "cycle-"
  key_links:
    - from: "crontab"
      to: "scripts/run_cycle.sh"
      via: "cron scheduling"
      pattern: "polymarket-trader"
---

<objective>
Validate the system by running 20+ autonomous paper trading cycles via cron over 2+ days, then collect evidence of strategy evolution, calibration tracking, and golden-rules expansion.

Purpose: Prove the complete autonomous trading system works end-to-end without human intervention.
Output: Evidence of 20+ successful cycles with strategy evolution
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-scheduling-paper-validation/06-01-SUMMARY.md
@.planning/phases/06-scheduling-paper-validation/06-02-SUMMARY.md
@polymarket-trader/scripts/install_cron.sh
@polymarket-trader/scripts/status.sh
@polymarket-trader/tools/validate_cycle.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install cron and verify initial operation</name>
  <files></files>
  <read_first>
    polymarket-trader/scripts/install_cron.sh
    polymarket-trader/scripts/status.sh
    polymarket-trader/scripts/run_cycle.sh
  </read_first>
  <action>
1. Install cron entries: `cd /home/trader/polymarket-trader && bash scripts/install_cron.sh`
2. Verify installation: `crontab -l | grep polymarket-trader` -- should show 3 entries
3. Run a manual test cycle to verify the system works: `bash scripts/run_cycle.sh --force`
4. Check status: `bash scripts/status.sh`
5. Verify the first cycle produced a report: `ls state/reports/cycle-*.md | head -1`

If the manual test cycle succeeds, the system is ready for unattended operation. The 3 cron entries will handle:
- Heartbeat every 10 minutes (zero-cost signal check)
- Gated cycle every 30 minutes (only runs when heartbeat signals work needed)
- Daily forced full scan at 2 AM UTC

Report to user: "Cron installed. System will accumulate cycles autonomously. Check back in 2+ days or run `scripts/status.sh` periodically."
  </action>
  <verify>
    <automated>cd /home/trader/polymarket-trader && crontab -l 2>/dev/null | grep -c "polymarket-trader"</automated>
  </verify>
  <acceptance_criteria>
    - `crontab -l | grep -c "polymarket-trader"` outputs 3
    - At least 1 cycle report exists in state/reports/
    - scripts/status.sh runs without error
  </acceptance_criteria>
  <done>Cron is installed with 3 entries, initial manual cycle succeeded, system is running unattended.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify 20+ autonomous cycles after 2+ days</name>
  <files></files>
  <action>
This is a human verification checkpoint. After 2+ days of unattended cron operation, the user verifies the system has accumulated 20+ autonomous paper trading cycles with evidence of strategy evolution. The executor presents the verification commands and results to the user.

Run these commands and present results to the user:
1. `cd /home/trader/polymarket-trader && bash scripts/status.sh`
2. `source .venv/bin/activate && python tools/validate_cycle.py --summary --pretty`
3. `python tools/enable_live.py --check`
  </action>
  <verify>
    <automated>cd /home/trader/polymarket-trader && source .venv/bin/activate && python tools/validate_cycle.py --summary 2>/dev/null | python3 -c "import sys,json; d=json.load(sys.stdin); print('PASS' if d.get('total_cycles',0) >= 20 else f'NEED MORE: {d.get(\"total_cycles\",0)} cycles')"</automated>
  </verify>
  <acceptance_criteria>
    - validate_cycle.py --summary reports total_cycles >= 20
    - validate_cycle.py --summary reports evolution_evidence = true
    - enable_live.py --check runs without error and shows 4 criteria
    - No systematic errors in state/run_cycle.log
  </acceptance_criteria>
  <done>20+ autonomous cycles completed, strategy evolution demonstrated, calibration tracking active, live gate mechanism verified.</done>
  <what-built>
Complete autonomous trading system running via cron: heartbeat every 10min, gated cycles every 30min, daily forced scan at 2AM UTC. The system has been accumulating paper trading cycles unattended.
  </what-built>
  <how-to-verify>
After 2+ days of unattended operation:

1. Check system status:
   ```
   cd /home/trader/polymarket-trader
   bash scripts/status.sh
   ```
   Expected: Total cycles >= 20

2. Run validation summary:
   ```
   source .venv/bin/activate
   python tools/validate_cycle.py --summary --pretty
   ```
   Expected output confirms:
   - total_cycles >= 20 (VAL-01)
   - rules_added > 0 OR evolution_evidence = true (VAL-04)
   - calibration_entries >= 0 (VAL-03, may be 0 if no resolutions yet)
   - strategy_lines_added > 0 (VAL-02)

3. Check live gate readiness:
   ```
   python tools/enable_live.py --check
   ```
   Expected: Shows all 4 criteria status (may not all pass yet -- that is OK, the gate mechanism being built and verifiable is VAL-05)

4. Spot-check a few recent cycle reports:
   ```
   ls -la state/reports/ | tail -5
   cat state/reports/$(ls state/reports/ | tail -1)
   ```
   Expected: Reports have proper structure (Summary, Markets Analyzed, etc.)

5. Check for errors:
   ```
   grep -i "error\|fail\|abort" state/run_cycle.log | tail -10
   ```
   Expected: No systematic errors (occasional individual market failures are OK)

If total_cycles < 20, wait longer and re-check. The 30-min cycle interval means ~48 potential cycles per day, but signal gating means actual cycles depend on market activity.
  </how-to-verify>
  <resume-signal>Type "approved" with cycle count, or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `crontab -l | grep -c "polymarket-trader"` returns 3
- `python tools/validate_cycle.py --summary` shows total_cycles >= 20
- `python tools/enable_live.py --check` runs without error and shows 4 criteria
- `bash scripts/status.sh` shows system health
</verification>

<success_criteria>
- 20+ cycle reports exist in state/reports/ (VAL-01)
- strategy.md shows evidence-backed growth (VAL-02)
- calibration tracking is active (VAL-03)
- golden-rules.md has expanded OR evolution_evidence is true (VAL-04)
- enable_live.py --check runs and shows 4 criteria status (VAL-05)
- System ran 2+ days without human intervention
</success_criteria>

<output>
After completion, create `.planning/phases/06-scheduling-paper-validation/06-03-SUMMARY.md`
</output>
