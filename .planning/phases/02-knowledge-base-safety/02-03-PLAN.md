---
phase: 02-knowledge-base-safety
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - polymarket-trader/knowledge/prior-strategy-archive.md
  - polymarket-trader/state/strategy.md
  - polymarket-trader/state/core-principles.md
autonomous: true
requirements: [STRAT-01, STRAT-02, SAFE-01, SAFE-03, SAFE-04, SAFE-05, SAFE-06, SAFE-07]
must_haves:
  truths:
    - "state/strategy.md contains only section headers with no pre-seeded rules"
    - "state/core-principles.md contains only immutable guardrails with percentage-based limits"
    - "knowledge/prior-strategy-archive.md preserves the 18 rules from the old strategy.md with cycle timestamps"
    - "core-principles.md enforces: paper default, 5% max position, 30% max exposure, live gate, no deletion, record-before-confirm, 5-loss pause"
  artifacts:
    - path: "polymarket-trader/knowledge/prior-strategy-archive.md"
      provides: "Archived prior-era strategy rules for reference"
      contains: "Market Selection Rules"
      min_lines: 40
    - path: "polymarket-trader/state/strategy.md"
      provides: "Clean strategy file for Claude to populate"
      contains: "## Market Selection Rules"
      min_lines: 10
    - path: "polymarket-trader/state/core-principles.md"
      provides: "Immutable safety guardrails"
      contains: "5% max position"
      min_lines: 30
  key_links:
    - from: "polymarket-trader/state/core-principles.md"
      to: "polymarket-trader/CLAUDE.md"
      via: "read at session start"
      pattern: "core-principles.md"
    - from: "polymarket-trader/state/strategy.md"
      to: "polymarket-trader/CLAUDE.md"
      via: "read and updated each cycle"
      pattern: "strategy.md"
---

<objective>
Archive existing strategy rules, reset strategy.md for Claude to build from scratch, and rewrite core-principles.md as a pure immutable guardrails file with percentage-based limits.

Purpose: Claude should start fresh with its own strategy discovery (not inherit prior-era rules as active strategy), while having battle-tested safety guardrails that cannot be overridden. The old strategy rules are preserved in an archive for reference.
Output: 3 files updated/created in polymarket-trader/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-knowledge-base-safety/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive strategy.md and reset to empty sections</name>
  <files>
    polymarket-trader/knowledge/prior-strategy-archive.md
    polymarket-trader/state/strategy.md
  </files>
  <read_first>
    polymarket-trader/state/strategy.md
  </read_first>
  <action>
Per D-10: Archive and reset strategy.md in two steps.

**Step 1 -- Archive to knowledge/prior-strategy-archive.md:**

Create `polymarket-trader/knowledge/prior-strategy-archive.md` with:
- Header: `# Prior Strategy Archive`
- Note: `Archived from state/strategy.md on {today's date}. These rules were developed during the multi-agent era (March-April 2026). Preserved as readable reference -- Claude may consult these for historical context but should develop its own strategy through autonomous trading.`
- Copy the ENTIRE current content of `polymarket-trader/state/strategy.md` below the header note, preserving all 18 rules with their cycle timestamps (e.g., "Added cycle 20260328-080810"), section headers, and the Cycle Health Tracking section.
- Do NOT add commentary or edit the archived content -- it is a raw copy of the current strategy.md.

**Step 2 -- Reset state/strategy.md:**

Overwrite `polymarket-trader/state/strategy.md` with section headers only. The file should contain exactly:

Line 1: `# Strategy`
Line 2: (blank)
Line 3: `This document is updated by the trading agent after each cycle.`
Line 4: `It starts blank and evolves based on trading experience.`
Line 5: `Claude builds all rules here through autonomous trading -- no pre-seeded content.`
Line 6: (blank)
Line 7: `## Market Selection Rules`
Line 8: (blank)
Line 9: `## Analysis Approach`
Line 10: (blank)
Line 11: `## Risk Parameters`
Line 12: (blank)
Line 13: `## Trade Entry/Exit Rules`
Line 14: (blank)
Line 15: `## Cycle Health Tracking`

No rules, no content under any section header. Claude writes everything from scratch through trading.
  </action>
  <verify>
    <automated>grep -c "Added cycle" polymarket-trader/knowledge/prior-strategy-archive.md | xargs test 5 -le && grep -c "Added cycle" polymarket-trader/state/strategy.md | xargs test 0 -eq && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - polymarket-trader/knowledge/prior-strategy-archive.md exists
    - prior-strategy-archive.md contains "Prior Strategy Archive"
    - prior-strategy-archive.md contains "Added cycle 20260328-080810" (first rule's timestamp preserved)
    - prior-strategy-archive.md contains "Market Selection Rules"
    - prior-strategy-archive.md contains "Analysis Approach"
    - prior-strategy-archive.md contains "Cycle Health Tracking"
    - prior-strategy-archive.md contains "multi-agent era" (archive context note)
    - polymarket-trader/state/strategy.md contains "## Market Selection Rules"
    - polymarket-trader/state/strategy.md contains "## Analysis Approach"
    - polymarket-trader/state/strategy.md contains "## Risk Parameters"
    - polymarket-trader/state/strategy.md contains "## Trade Entry/Exit Rules"
    - polymarket-trader/state/strategy.md does NOT contain "Added cycle" (no pre-seeded rules)
    - polymarket-trader/state/strategy.md does NOT contain "slippage" (no old rule content)
    - polymarket-trader/state/strategy.md is under 20 lines (headers only)
  </acceptance_criteria>
  <done>Old strategy archived with all 18 rules and timestamps preserved, strategy.md reset to empty section headers only</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite core-principles.md as pure immutable guardrails</name>
  <files>polymarket-trader/state/core-principles.md</files>
  <read_first>
    polymarket-trader/state/core-principles.md
  </read_first>
  <action>
Per D-04, D-05, D-06: Completely rewrite `polymarket-trader/state/core-principles.md` as a pure immutable guardrails file.

**REMOVE all session-specific content (per D-05):**
- Remove "Session Mode: Overnight Crypto Sprint" section entirely
- Remove crypto-focused parameters (crypto correlation control, category focus, overnight liquidity)
- Remove fixed dollar amounts ($25, $200, $5 min order)
- Remove 1-hour cycle interval
- Remove 72h resolution hard limit (that was session-specific)
- Remove numbered principles 1-24 format -- replace with focused guardrails

**Write the file with this exact content (use `***` for horizontal rules between sections):**

The file must start with:

`# Core Principles`

Followed by: `These principles are set by the human operator and are immutable. The agent reads this file at cycle start but NEVER modifies it. Any attempt to write to this file must hard-fail.`

Then a `## Reality Check` section with: `85-90% of retail accounts on Polymarket end with net losses. The top 0.04% of wallets capture 70%+ of all profits. This agent exists to be in the other 10-15%. Every guardrail below serves that single goal.`

Then `## Immutable Guardrails` with these 7 numbered subsections:

`### 1. Paper Trading Default` -- Execution mode is PAPER until the operator explicitly enables live trading via `python tools/enable_live.py --activate`. Live trading requires passing all gate criteria. Claude NEVER switches to live mode on its own. (Implements SAFE-01)

`### 2. Maximum 5% Position Size` -- No single position may exceed 5% of current bankroll. Bankroll is read from `state/bankroll.json` at cycle start. If bankroll.json is missing, assume $10,000 default. This is a HARD cap -- never override upward regardless of confidence or edge. (Implements SAFE-03)

`### 3. Maximum 30% Total Exposure` -- Total open position exposure (sum of all position costs) must not exceed 30% of current bankroll. If adding a new position would breach this limit, skip the trade. Check exposure BEFORE every trade execution. (Implements SAFE-04)

`### 4. Live Trading Gate` -- Live trading requires ALL of: (a) 10+ completed paper trading cycles, (b) positive cumulative P&L, (c) win rate above 50%, (d) explicit operator confirmation via enable_live.py. If ANY criterion fails, remain in paper mode.

`### 5. No Deletion of Audit Trail` -- Cycle reports in `state/reports/`, calibration history in `knowledge/calibration.json`, and trade records in `trading.db` must NEVER be deleted. Append only. If a file is corrupted, create a new file with a timestamp suffix -- do not overwrite the original. (Implements SAFE-07)

`### 6. Record Before Confirm` -- Every trade must be recorded in the database (via execute_trade.py) BEFORE the agent considers it confirmed. If the recording fails, the trade did not happen. Never report a trade as executed without a database record. (Implements SAFE-06)

`### 7. Five-Loss Trading Pause` -- After 5 consecutive losing trades (resolved positions with negative P&L), the agent must pause all new trade execution for 24 hours. During the pause: continue scanning, analyzing, and writing reports -- but execute zero trades. Log the pause in the cycle report. Resume after 24 hours from the 5th loss resolution timestamp. (Implements SAFE-05)

Then `## Sizing Reference` with a markdown table:

| Parameter | Value |
|-----------|-------|
| Max single position | 5% of bankroll |
| Max total exposure | 30% of bankroll |
| Kelly fraction | 0.25 (quarter-Kelly) |
| Min edge (default) | 0.04 (4pp) |
| Price sweet spot | YES price 0.15 - 0.85 |

Followed by: `Position sizes are calculated as bankroll percentages. The actual dollar amount is derived from state/bankroll.json at runtime.`

Then `## Category Size Caps` with a markdown table:

| Category | Max Per Bet | Min Edge | Notes |
|----------|-------------|----------|-------|
| Crypto | 3% bankroll | 5pp | High variance, high correlation |
| Politics | 3% bankroll | 4pp | Status quo bias applies |
| Sports | 2% bankroll | 4pp | Single games only; 3% for season-long |
| Commodities | 4% bankroll | 4pp | Settlement != intraday |
| Entertainment | 2% bankroll | 8pp | Calibration probation (Oscars loss) |
| Finance | 3% bankroll | 4pp | Near-certainty events have no edge |

Then `## Execution Realism (Paper Mode)` with: Paper fills must use the order book: BUY at best_ask, SELL at best_bid. Quantize to tick_size. If the book is stale (>10s old), skip. No fills at mid-price. Apply actual CLOB fee schedules. Fees changed 2026-03-30 -- use current schedule.

Then `## File Discipline` with:
- NEVER modify: `.env`, `trading.db` (directly), `lib/config.py`, `state/core-principles.md`
- Cycle outputs go to `state/cycles/{cycle_id}/`
- Cycle reports go to `state/reports/cycle-{cycle_id}.md`
- All intermediate files kept for audit trail

Use horizontal rules (`***`) between major sections for readability.
  </action>
  <verify>
    <automated>grep -c "Immutable Guardrail" polymarket-trader/state/core-principles.md | grep -q "1" && grep "5%" polymarket-trader/state/core-principles.md | head -1 | grep -q "5%" && grep "30%" polymarket-trader/state/core-principles.md | head -1 | grep -q "30%" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - polymarket-trader/state/core-principles.md contains "## Immutable Guardrails"
    - File contains "### 1. Paper Trading Default"
    - File contains "### 2. Maximum 5% Position Size"
    - File contains "### 3. Maximum 30% Total Exposure"
    - File contains "### 4. Live Trading Gate"
    - File contains "### 5. No Deletion of Audit Trail"
    - File contains "### 6. Record Before Confirm"
    - File contains "### 7. Five-Loss Trading Pause"
    - File contains "5% of bankroll" (percentage-based, per D-04)
    - File contains "30% of bankroll" (percentage-based, per D-04)
    - File contains "bankroll.json" (runtime bankroll reference)
    - File does NOT contain "Overnight Crypto Sprint" (session content removed per D-05)
    - File does NOT contain "$25" as a max trade value (fixed dollar amounts removed per D-04)
    - File does NOT contain "$200" as max exposure (fixed dollar amounts removed per D-04)
    - File contains "NEVER modifies" or "NEVER modified" or "never writes" (immutability statement)
    - File contains "Reality Check" (preserved motivational opening)
    - File contains "Category Size Caps"
    - File contains "## File Discipline"
  </acceptance_criteria>
  <done>core-principles.md rewritten as pure immutable guardrails with 7 numbered safety rules, percentage-based limits, no session-specific content, category size caps table</done>
</task>

</tasks>

<verification>
- `grep -c "Added cycle" polymarket-trader/state/strategy.md` returns 0 (no pre-seeded rules)
- `grep "## Market Selection Rules" polymarket-trader/state/strategy.md` finds section header
- `grep "Prior Strategy Archive" polymarket-trader/knowledge/prior-strategy-archive.md` finds header
- `grep "Immutable Guardrails" polymarket-trader/state/core-principles.md` finds section
- `grep "5%" polymarket-trader/state/core-principles.md` finds position limit
- `grep "30%" polymarket-trader/state/core-principles.md` finds exposure limit
</verification>

<success_criteria>
- prior-strategy-archive.md preserves all 18 rules with cycle timestamps as readable reference
- strategy.md contains only section headers (Market Selection Rules, Analysis Approach, Risk Parameters, Trade Entry/Exit Rules, Cycle Health Tracking) with no content
- core-principles.md has exactly 7 immutable guardrails covering all SAFE requirements
- All sizing is percentage-based (no fixed dollar amounts)
- No session-specific content (no overnight sprint, no crypto focus, no 72h hard limit)
</success_criteria>

<output>
After completion, create `.planning/phases/02-knowledge-base-safety/02-03-SUMMARY.md`
</output>
